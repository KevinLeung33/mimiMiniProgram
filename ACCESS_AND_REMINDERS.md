# 后续规划：每日提醒与仅限特定人使用

## 每日记录提醒

可以实现，但分两层：

1. **当前 Demo**：首页会显示未完成的当天记录和护理待办；不需要联网。
2. **正式提醒**：使用微信订阅消息 + 云端定时任务。用户需要在小程序内主动点一次“订阅明天的记录提醒”，授权后，云端在约定时间检查是否已经记录，再发送一条消息。

微信订阅消息不是可以永久、无授权地每天推送的普通通知。一次性订阅的授权对应一次发送额度，因此产品上应设计成：用户每次完成当天记录后，可以顺手授权“明天提醒我”。云端定时任务负责检查与发送。[CloudBase 订阅消息云函数指引](https://docs.cloudbase.net/en/recipes/add-subscribe-message-cloud-function)

第一版不要先做推送；先确认你和女朋友是否会每天打开它。之后再接：订阅模板、云数据库、定时云函数和提醒设置页。

## 只给你、女朋友和少量测试朋友使用

可以，而且建议按下面的阶段做：

### 开发与测试期

- 不公开发布，只使用开发版、体验版或预览码；
- 只把测试资格给你、女朋友和明确的测试朋友；
- 测试数据使用演示数据或已获得同意的照片。

### 私人正式版

把本地数据迁移到 CloudBase，并增加一个“猫咪家庭”白名单：

```text
owner：你
member：女朋友
tester：临时测试朋友（可设置过期时间）
```

每位进入者都有自己的微信 OpenID；后端只向白名单成员返回这个家庭的记录和素材。云数据库支持按 OpenID 与安全规则限制读写；私密数据不应使用“所有人可读”的默认权限。[CloudBase 小程序数据库权限说明](https://docs.cloudbase.net/en/recipes/add-database-wechat-miniprogram)、[安全规则文档](https://docs.cloudbase.net/database/security-rules)

对于“你和女朋友共同维护同一只猫”的共享数据，推荐由云函数检查家庭白名单后再读写，而不是简单地让每个用户只能看到自己创建的数据。

## 发布前的原则

- 默认私密，不做公开搜索、公开分享或陌生人注册；
- 照片和记录只有白名单用户可见；
- 测试者可以随时移出白名单；
- 提供“删除记录 / 删除照片 / 导出数据”的入口；
- 不把猫砂和湿粮记录描述为医疗诊断。
