# 小程序初版：你和我分别要做什么

这份顺序以「可以每天记录、两个人共享、还能玩找猫小剧场」为目标。不要同时做 AI 总结、视频、大量场景或复杂素材后台。

## 初版范围先锁死

初版只包含：

- 日常记录：猫砂、湿粮、护理、备注、每天一张照片；
- 小剧场：窗边、沙发、纸箱三个场景；
- 玩法：猫藏在一个已保存的位置，找到后互动；某些回应会让猫移动；
- 每个场景两个互动、每个互动两种回应；
- 图片反馈优先，短视频以后再加；
- 两个人共享同一个「猫咪之家」和各自的提醒授权。

初版暂时不包含：

- AI 日记总结；
- 视频批量上传或剪辑；
- 在小程序内编辑素材的复杂后台；
- 多只猫、排行榜、战斗或独立小游戏。

## 执行顺序

| 顺序 | 你要做 | 我来做 | 完成标志 |
| ---: | --- | --- | --- |
| 1 | 确认猫咪昵称、三处场景、六个互动动作、提醒时间 | 根据你的选择锁定配置名称 | 不再改动作 ID 和场景 ID |
| 2 | 按 MATERIAL_MANIFEST.md 拍好或挑好猫咪、房间参考图 | 检查参考图是否足以识别猫咪和房间 | 7 张猫咪参考图 + 3 张房间参考图 |
| 3 | 按 ASSET_PROMPTS.md 先生成并确认风格母版 | 帮你检查是否像同一只猫、是否适合作为统一基准 | cat-style-master-v01.png 已确认 |
| 4 | 依次生成 3 张背景、3 张 found 图、12 张互动回应图 | 根据清单核对命名、尺寸、透明背景与移动方向 | 19 项核心物料完成 |
| 5 | 把 approved 最终文件放进指定素材目录 | 把图片接入现有页面，改成「随机藏猫 → 寻找 → 互动 → 可能移动」 | 本地 Demo 可完整游玩 |
| 6 | 创建小程序 AppID 和一个云开发环境；不要提供账号密码或密钥 | 把本地记录迁到云端数据结构，加入家庭成员关系 | 两台手机读到同一份记录 |
| 7 | 你和女朋友分别在自己的手机测试记录、照片、找猫和提醒授权 | 修复测试中发现的流程问题 | 连续使用 7 天无明显阻塞 |
| 8 | 决定是否需要视频、素材后台或 AI 周报 | 在真实使用反馈后再扩展 | 初版稳定，不被功能拖慢 |

## 你今天只需要完成三件事

1. 在 MATERIAL_MANIFEST.md 的「固定设定」填入猫咪昵称和每天提醒时间；
2. 选出三张房间参考图，按 room-window-01、room-sofa-01、room-box-01 命名；
3. 选出四张最清楚的猫咪身份参考图，按 cat-id-01 至 cat-id-04 命名。

完成后，不要立刻生成所有回应图。先用 P-00 做风格母版；确认「这张猫看起来就是它」之后，再批量生成后面的素材，返工会少很多。

## 开发阶段的关键规则

### 随机藏猫不是每次打开都重抽

每天第一次进入小剧场，或猫在互动后跑走时，才更新一次当前位置。位置要保存下来；你和女朋友看到的是同一只猫、同一个位置。

~~~text
进入小剧场
→ 读取今天猫的位置
→ 选一个场景寻找
→ 找到后互动
→ 留在原地，或移动到预设的另一个场景
~~~

第一版不做寻路动画，只显示「它跑去纸箱了」并更新位置。这既有游戏感，也不会增加不必要的开发量。

### 物料和源文件分开

- 原始猫咪照片、房间照片、AI 草稿：留在 cat-diary-material-source；
- 确认的导出文件：复制到小程序的 assets/scenes 和 assets/reactions；
- 不要用同名文件覆盖旧版；新导出统一升到 v02。

### 云端共享放在视觉版跑通之后

先让物料和玩法在本地 Demo 中成立，再接云端共享、照片保存和订阅提醒。这样你不会在账号、权限和素材问题之间同时卡住。

## 我需要你交付给我的东西

不用提供任何账号密码、微信登录信息或密钥。需要的是：

- 已确认的最终素材文件；
- 已填好的 MATERIAL_MANIFEST.md，或告诉我哪些行有变化；
- 小程序 AppID 和云开发环境 ID（进入云端阶段时）；
- 你和女朋友各自希望收到提醒的时间。

收到这些后，我会负责代码层的素材接入、找猫状态、互动移动逻辑、云端家庭数据和提醒流程。
