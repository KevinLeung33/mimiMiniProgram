# 后续规划：AI 日记总结（暂不实现）

## 目标

让 AI 把一周或一个月的**已记录事实**整理成温柔、可读的猫咪周报，而不是做健康诊断。

## 推荐的三阶段

### 阶段 A：先做非 AI 周报

按程序规则直接汇总：

- 各尺寸尿团和屎团的记录次数；
- 湿粮出现次数与“吃完 / 部分吃 / 不吃”的比例；
- 已完成的护理事项；
- 用户写下的备注。

这一步能验证“你真正想看什么”，并建立可检查的事实底座。

### 阶段 B：手动触发 AI 总结

在“周报”页添加一个按钮：`生成本周小结`。只把结构化汇总和文字备注发送给模型，默认不发送原始照片。

建议要求模型输出固定四段：

```text
本周发生了什么（仅复述记录）
湿粮偏好观察（只描述样本内现象）
已完成的照护事项
值得你回看的一件小事
```

模型提示词必须包含：

```text
不要诊断疾病，不要给出用药、护理频率或医疗建议；数据不足时明确说“记录不足，无法判断”；不要把一次记录解释为长期趋势。
```

### 阶段 C：可控的长期分析

- 允许选择统计范围：7 天、30 天、自定义；
- 允许选择语气：客观、温柔、简短；
- 记录“这段总结是否有用”，用于改进提示词；
- 允许用户删除 AI 摘要和原始数据。

## 技术路线（以后）

当小程序从本地 Demo 升级到云端后，可以使用 CloudBase 存储记录，再由云函数或小程序端在用户点击后调用文本模型。CloudBase 当前支持在小程序中接入文本生成模型，也支持通过云端保存数据、文件和身份信息。[CloudBase AI 接入指引](https://cloud.tencent.com/document/product/876/116226)、[CloudBase 产品概述](https://cloud.tencent.com/document/product/876/18431)

AI 总结应始终是用户主动点击产生的附加功能，不应代替兽医意见，也不应把普通日记数据包装成健康结论。
